import { GymGoalLabelPipe } from './gym-goal-label.pipe';

describe('GymGoalLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new GymGoalLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
